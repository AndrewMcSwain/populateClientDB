function interestingObject(objectOfInterest){
   this.objectOfInterest.name = name;
   this.objectOfInterest.type = type;
   this.objectOfInterest.size = size;
   this.objectOfInterest.danger = danger;
   this.objectOfInterest.material = material;
   this.objectOfInterest.description = description;
}

var cobSlide = {
  name: "cobSlude"
  type: "adrenaline producer",
  size: "super big",
  danger: "pretty safe",
  material: "mud and clay",
  description: "slippery when wet",
} 




/**
function AutoPopulate(objectOfInterest){
  populatorStud(cobSlide);

function populatorStud(stud) {
let rocket = {}
 stud.forEach(key, rocket.key = key)

